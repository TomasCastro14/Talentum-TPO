version: '3.8'

services:
  neo4j:
    image: neo4j:5.18.0-community
    container_name: neo4j-tpo
    restart: always
    ports:
      - "7474:7474" # Neo4j Browser
      - "7687:7687" # Bolt port
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
      - neo4j_import:/var/lib/neo4j/import
    environment:
      # Configuración de autenticación
      NEO4J_AUTH: neo4j/password
      # Aceptar la licencia (requerido para versiones no-community)
      # NEO4J_ACCEPT_LICENSE_AGREEMENT: 'yes'
    networks:
      - neo4j-network

volumes:
  neo4j_data:
  neo4j_logs:
  neo4j_import:

networks:
  neo4j-network:
    driver: bridge